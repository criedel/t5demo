<!DOCTYPE html>
<body xmlns:t="http://tapestry.apache.org/schema/tapestry_5_3.xsd"
      xmlns:p="tapestry:parameter"
      t:type="UILayout">

    <div t:type="zone" t:id="settingsZone" id="settingsZone">
        <t:headline t:text="headline" />

        <t:alerts />

        <form t:id="settingsForm"
              t:type="beanEditForm"
              t:zone="settingsZone"
              t:object="user"
              t:include="password,passwordRepeat" />
    </div>

</body>